# SupplyChainAgent Frontend Environment Configuration

# Application Execution Environment
# Supported values: development | staging | production | demo
VITE_APP_ENV=development

# Backend API Base URL (default connects to local FastAPI proxy)
# VITE_API_BASE_URL=http://127.0.0.1:8000
